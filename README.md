# Smart-ambulance
A Smart Ambulance Management System built in C++ that models a city as a graph and uses Dijkstra’s algorithm to find shortest routes. It allocates the nearest ambulance, selects the closest hospital, and prioritizes traffic signals to ensure faster emergency response.
